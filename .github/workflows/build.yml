name: Build and Deploy to Netlify
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: Checkout

      - uses: actions/setup-node@v4
        name: Setup Node.js
        with:
          node-version: "22"

      - run: npm ci
        name: Install dependencies

      - run: npm run build
        name: Build project

      - run: npx netlify-cli deploy --no-build --dir=dist --prod -s 35a65cf5-9272-4ca1-8e51-60ea84c05a46 --auth ${{ secrets.NETLIFY_AUTH_TOKEN }} --message "Deployed commit ${{ github.sha }}"
        name: Deploy to Netlify
